import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rxmind.app',
  appName: 'RxMind',
  webDir: 'out', // Back to 'out' for static export
};

export default config;
